#!/usr/bin/env bash
# Direnv configuration for homelab Ansible repository
# Automatically loaded when entering this directory

# Ansible vault password file location
export ANSIBLE_VAULT_PASSWORD_FILE=~/.ansible_vault_pass

# Ansible configuration (auto-detected from ansible.cfg in repo root)
export ANSIBLE_CONFIG="$(pwd)/ansible.cfg"

# Default inventory (can override with -i flag)
export ANSIBLE_INVENTORY="$(pwd)/inventories/production/hosts.ini"

# Vault secrets file location (for reference)
export HOMELAB_VAULT_FILE="$(pwd)/vault/secrets.yaml"

# Display loaded configuration
echo "🏠 Homelab Ansible environment loaded:"
echo "  🔐 Vault password: $ANSIBLE_VAULT_PASSWORD_FILE"
echo "  ⚙️  Config: $ANSIBLE_CONFIG"
echo "  📋 Inventory: $ANSIBLE_INVENTORY"
echo "  🔒 Vault file: $HOMELAB_VAULT_FILE"
