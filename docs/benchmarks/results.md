# Terminal-Bench Results

Appended automatically by `playbooks/individual/ocean/ai/terminalbench.yaml`
after each run. One row per `(date, model)` evaluation; the matching
full result JSON lives under `docs/benchmarks/runs/`.

| date | model | dataset | agent | pass_rate | resolved | total | errored | duration_s |
|------|-------|---------|-------|-----------|----------|-------|---------|------------|
