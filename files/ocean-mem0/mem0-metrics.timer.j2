[Unit]
Description=Collect mem0 statistics every minute

[Timer]
OnBootSec=2min
OnUnitActiveSec=1min
# Not Persistent: these are gauges of the present, so a missed run while the
# host was down is not worth replaying on boot.
AccuracySec=10s

[Install]
WantedBy=timers.target
