#!/usr/bin/env bash
# Automatically set Ansible vault password file for homelab playbooks
export ANSIBLE_VAULT_PASSWORD_FILE=~/.ansible_vault_pass

# Optional: Add other homelab-specific environment variables
# export ANSIBLE_CONFIG=ansible.cfg
# export ANSIBLE_INVENTORY=inventory.ini

echo "🔐 Ansible vault password file set: $ANSIBLE_VAULT_PASSWORD_FILE"
